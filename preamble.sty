\ProvidesPackage{preamble}

\PassOptionsToPackage{hyphens}{url}
\usepackage[pdftex]{hyperref}
\hypersetup{
    colorlinks=true,
    urlcolor=blue,
    }
\urlstyle{same}
\usepackage [utf8]{inputenc}
\usepackage[margin=1.905cm]{geometry}
\usepackage{mathptmx}
\usepackage{ulem}
\usepackage[none]{hyphenat} % This style needs to be in the repo, It's not in BasicTex
\usepackage{fancyhdr}
\fancyhf{}
\fancyhead[L]{973.906.0496}
\fancyhead[C]{\textbf{Christopher M. Finazzo}}
\fancyhead[R]{chris@chrisfinazzo.com}
\cfoot{\textit{\VCDateISO - \GITAbrHash}}
\setlength{\headsep}{0.762cm}
\thispagestyle{plain}
\pagestyle{fancy}
\usepackage{enumitem} % This style needs to be in the repo, It's not in BasicTex
\newcommand{\companydetails}[1]{%
  \par\addvspace{\bigskipamount}
  \noindent
  \begin{tabular}[t]{ @{} l @{} }
    \strut #1
  \end{tabular}}
\newcommand{\companyduration}[1]{%
  \hfill
  \strut #1
}
\pagenumbering{gobble}
\def\myauthor{Christopher M. Finazzo}
\def\mycopyright{2019}
\endinput
